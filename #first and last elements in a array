

value1 = int(input("enter the integer "))
b = []
for i in range(0,value1):
    temp = int(input())
    b.append(temp)
    print(b)
    
size = len(b)

temp = len(b)

b[0] = b[size-1]

b[size-1] = temp

print("list after swaping",b)
